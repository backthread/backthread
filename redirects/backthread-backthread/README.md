# @backthread/backthread → use `backthread`

This name is a redirect. The Backthread CLI is published under the unscoped name [`backthread`](https://www.npmjs.com/package/backthread):

```bash
npx backthread
```
